
## Graph - structures

**neighbors**: directly connected nodes. Note that these may differ from the geographical - neighbors. When self-loops are included, the set of neighbors of node $n$ includes node $n$ itself.
**neighborhood**: the set of neighbors of a node $n$. When self-loops are included, the neighbors of node $n$ is equal to its neighborhood.